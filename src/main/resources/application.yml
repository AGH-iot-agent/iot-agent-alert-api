server:
  port: 8080

spring:
  application:
    name: iot-agent-alert-api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
